{
  "name": "foxypowers",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "files": [
    "commands",
    "skills",
    "src"
  ],
  "scripts": {
    "postinstall": "cfg=\"${XDG_CONFIG_HOME:-$HOME/.config}/opencode\"; mkdir -p \"$cfg/commands\" && cp -f commands/*.md \"$cfg/commands/\""
  },
  "opencode": {
    "skills": [
      "skills/git-worktree-guidance",
      "skills/docs-maintenance",
      "skills/claude-compat-init"
    ]
  }
}
